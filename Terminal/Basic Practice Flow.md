Here’s a **hands-on, step-by-step Linux practice set** designed to be solved **in sequence**, where each task builds on the previous one. This mirrors how Linux is usually practiced in real environments.

---

## 🐧 Linux Command Practice Set (Beginner → Practice Flow)

> **Rule:** Do not skip steps. Use only the commands from the given list whenever possible.

---

### 🔹 PART 1: Navigation & Environment

1. Display your **current working directory**.
   *(Hint: find where you are)*

2. List all files and directories in your current location.

3. Create a new directory called `linux_practice`.

4. Move into the `linux_practice` directory.

5. Confirm that you are inside `linux_practice`.

---

### 🔹 PART 2: File Creation & Viewing

6. Create an empty file named `file1.txt`.

7. Create another empty file named `file2.txt`.

8. Display the contents of `file1.txt`.

9. Write the text

   ```
   Linux is powerful
   ```

   into `file1.txt`.

10. Display the contents of `file1.txt` again to confirm.

---

### 🔹 PART 3: Copying, Moving & Linking

11. Copy `file1.txt` to a new file called `file1_backup.txt`.

12. Rename `file2.txt` to `notes.txt`.

13. Create a **hard link** to `file1.txt` named `file1_link.txt`.

14. List files to verify all changes.

---

### 🔹 PART 4: Searching & Text Processing

15. Search for the word `Linux` inside `file1.txt`.

16. Count the number of **lines, words, and characters** in `file1.txt`.

17. Sort the contents of `file1.txt` alphabetically.

---

### 🔹 PART 5: System & User Information

18. Display your **username**.

19. Show information about your Linux system/kernel.

20. Display the current month’s calendar.

21. Show currently running processes.

---

### 🔹 PART 6: Disk, Network & Utilities

22. Display disk space usage of the system.

23. Find the location of the `ls` command.

24. Clear the terminal screen.

25. Download any small text file from the internet using a URL of your choice.

---

### 🔹 PART 7: Cleanup Tasks

26. Delete the file `file1_backup.txt`.

27. Delete the file `notes.txt`.

28. Move back to the parent directory.

29. Remove the `linux_practice` directory **only if it is empty**.

---

### 🔹 BONUS CHALLENGE (Optional)

30. Use the manual pages to find:

* What option of `ls` shows hidden files?
* What option of `rm` asks for confirmation before deleting?

---

## ✅ Commands Covered

`ls, pwd, mkdir, cd, touch, cat, echo, cp, mv, ln, grep, wc, sort, whoami, uname, cal, ps, df, whereis, clear, wget, rm, rmdir, man`

---

If you want, I can also:

* ✅ Provide **solutions step-by-step**
* 🧪 Turn this into a **Linux lab worksheet**
* 🏆 Create **interview-style command challenges**

Just tell me 👍
